'use client';

import { useState } from 'react';

export default function Todo() {
  // This component is not currently used in the portfolio
  // It's just a placeholder since it's imported in the page
  return null;
} 